import { FirstLetterMajPipe } from './first-letter-maj.pipe';

describe('FirstLetterMajPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstLetterMajPipe();
    expect(pipe).toBeTruthy();
  });
});
